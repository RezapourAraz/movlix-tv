import React from 'react';
import Layout from '../../components/layout/Layout';

const CatalogPage = () => {
    return (
        <Layout>
            Catalog page
        </Layout>
    );
};

export default CatalogPage;